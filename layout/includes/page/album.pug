#album
  .author-content.author-content-item.album.single(style = `background: linear-gradient(rgba(0, 0, 0, 0.25), rgba(0, 0, 0, 0.25)), url(${theme.album.top_background}) center calc(50% + 3%) / cover no-repeat;` : "")
    .card-content
      .author-content-item-tips 圖片庫
      span.author-content-item-title 這裡是我的圖片庫哦😯
      .content-bottom
        .tips 各種展示。
      .banner-button-group
        a.banner-button(onclick='pjax.loadUrl("/about/")')
          i.anzhiyufont.anzhiyu-icon-arrow-circle-right(style='font-size: 1.5rem')
          span.banner-button-text 關於我
  .card-album
    each i in site.data.album
      .card(onclick=`pjax.loadUrl("${i.path_name}")`)
        img.card_cover(src=i.cover)
        .card__content
          p.card__category=i.class_name
          h3.card__heading=i.description
    .album-content-nocover
